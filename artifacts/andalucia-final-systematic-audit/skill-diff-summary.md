# Diff registrado — `solucion-de-ejercicios`

Comparación reproducible entre `skill-backup/` y la copia activa en
`.agents/skills/solucion-de-ejercicios/`.

Cambios de esta fase:

- obliga a representar integrales definidas y la regla de Barrow mediante
  MathML nativo con límites ligados al operador;
- prohíbe construir manualmente esa geometría con HTML/CSS;
- exige comprobar la geometría real del DOM a 1280, 768, 375 y 320 px;
- añade Extraordinaria 2020 como control de área entre `f(x)=|x|` y
  `g(x)=x²−2`, con cortes `(-2,2)` y `(2,2)` y una única gráfica exacta;
- eleva a barrera de publicación cualquier disposición manual de límites o
  error geométrico en Barrow.

Los hashes exactos anterior/nuevo de los dos archivos figuran en
`skill-hashes.json`; los archivos anteriores completos están en
`skill-backup/`.
