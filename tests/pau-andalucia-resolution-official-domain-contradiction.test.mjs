import test from 'node:test';import assert from 'node:assert/strict';import {buildDomainContradictionBatch} from '../scripts/register-andalucia-official-domain-contradiction.mjs';
test('real-domain contradiction is documentary and parameter-independent, never missing answer blockade',()=>{const b=buildDomainContradictionBatch(),r=b.blockedRecords[0];assert.equal(b.records.length,0);assert.equal(b.blockedRecords.length,1);assert.deepEqual(r.blockerCodes,['OFFICIAL_REAL_DOMAIN_CONTRADICTION']);assert.ok(r.sourceLiteral.includes('ln(ex + x3)'));assert.ok(r.blockerEvidence.counterexample.argument<0);assert.ok(Number.isNaN(Math.log(r.blockerEvidence.counterexample.argument)));for(const x of [-1,-2,-3])assert.ok(Math.exp(x)+x**3<0);assert.deepEqual(buildDomainContradictionBatch(),b);});
