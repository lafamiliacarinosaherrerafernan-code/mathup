import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';import {observations,cases,buildIntegralAbsoluteCubicEquilateralBatch} from '../scripts/resolve-andalucia-integral-absolute-cubic-equilateral.mjs';
test('0478 official PDF and inspected page hashes',()=>{const rows=p=>fs.readFileSync(p,'utf8').trim().split(/\r?\n/).map(JSON.parse),q=rows('artifacts/pau-andalucia-mass-processing/resolution-queue.jsonl'),m=new Map(rows('artifacts/pau-canonical-andalucia-madrid/runs/run-a/andalucia-canonical-exercises.jsonl').map(x=>[x.exerciseId,x])),sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');for(const o of observations){const e=m.get(q[o[0]].exerciseId);assert.equal(sha(e.provenance.localPath),o[1]);assert.equal(e.sourceRange.page,o[2]);assert.equal(sha(`tmp/pdfs/inference-source-glyphs/q${o[0]}.png`),o[4]);}});
test('1096 primitive differentiates numerically to f and passes through (2,6)',()=>{const F=x=>x*x/2+2*x+3*Math.log(Math.abs(x-1))-1/(x-1)+1,f=x=>x**3/(x-1)**2;assert.equal(F(2),6);for(const x of [-2,.2,2,4]){const h=1e-6,d=(F(x+h)-F(x-h))/(2*h);assert.ok(Math.abs(d-f(x))<1e-5);}});
test('1097 and 1101 derivative sign data and special values are exact',()=>{const f=x=>Math.abs(x*x-4);assert.deepEqual([-2,0,2].map(f),[0,4,0]);const g=x=>x**3-x*x-x+1;assert.ok(Math.abs(g(-1/3)-32/27)<1e-12);assert.equal(g(1),0);assert.ok(Math.abs(g(1/3)-16/27)<1e-12);});
test('1100 both points satisfy the three equal-distance equations',()=>{for(const z of [Math.sqrt(55)/2,-Math.sqrt(55)/2]){const P=[1.5,0,z],A=[4,0,0],B=[0,2,0],d2=(u,v)=>u.reduce((s,x,i)=>s+(x-v[i])**2,0);assert.ok(Math.abs(d2(P,A)-20)<1e-12);assert.ok(Math.abs(d2(P,B)-20)<1e-12);assert.equal(P[1],0);}});
test('0478 records preserve slots, four options, full solutions and order invariance',()=>{const a=buildIntegralAbsoluteCubicEquilateralBatch('test').batch,b=buildIntegralAbsoluteCubicEquilateralBatch('test',[...cases].reverse()).batch;assert.equal(a.records.length,4);assert.equal(a.records.flatMap(r=>r.parts).length,6);assert.deepEqual(a.records,[...b.records].reverse());for(const r of a.records){assert.equal(r.qualityGates.publication,'NOT_ENABLED');assert.equal(r.qualityGates.rendering,'RECHECK_REQUIRED');for(const p of r.parts){assert.ok(p.solutionSteps.length>=6);assert.equal(new Set([p.answer,...p.distractors]).size,4);assert.equal(p.answer,p.finalAnswer);}}});
